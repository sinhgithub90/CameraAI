[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "camera-ai"
version = "0.1.0"
description = "Camera-AI core module: YOLO detection + Qwen-VL scene analysis (Ollama)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
